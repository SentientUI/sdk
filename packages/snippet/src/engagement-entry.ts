// engagement.global.js: section attention + interaction capture, loaded by
// the snippet at boot in parallel with the decide (see startSectionCapture).
export { startEngagementCapture } from '@sentientui/core/engagement';
